const initialState = {
    readingList: [],
    discoverList: [],
    finishedList: []
}

const bookReducer = (state, action) => {

    switch (action.type) {
        case 'ADD_TO_READING_LIST': {

        }

        case 'REMOVE_FROM_READING_LIST': {

        }

        default: {

        }
    }

};
